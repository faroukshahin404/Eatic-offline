import 'package:flutter/material.dart';

class TabletHomeWidget extends StatelessWidget {
  const TabletHomeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Home Tablet"));
  }
}