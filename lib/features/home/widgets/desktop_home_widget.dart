import 'package:flutter/material.dart';

class DesktopHomeWidget extends StatelessWidget {
  const DesktopHomeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Home Desktop"));
  }
}